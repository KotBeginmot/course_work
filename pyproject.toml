[tool.poetry]
name = "1"
version = "1"
description = "1"
authors = ["1"]
license = "1"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
